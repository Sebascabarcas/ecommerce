import { NoCoverImagePipe } from './no-cover-image.pipe';

describe('NoCoverImagePipe', () => {
  it('create an instance', () => {
    const pipe = new NoCoverImagePipe();
    expect(pipe).toBeTruthy();
  });
});
